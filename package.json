{
  "name": "retro.css",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "yarn gulp",
    "dev": "concurrently \"yarn build\" \"(cd docs && yarn dev);\" -n sass,docs -c magenta,cyan"
  },
  "devDependencies": {
    "concurrently": "^7.2.1",
    "del": "^6.1.1",
    "gulp": "^4.0.2",
    "gulp-cache": "^1.1.3",
    "gulp-cssnano": "^2.1.3",
    "gulp-imagemin": "^7.0.0",
    "gulp-rename": "^2.0.0",
    "gulp-sass": "^5.1.0",
    "sass": "^1.52.2"
  }
}
